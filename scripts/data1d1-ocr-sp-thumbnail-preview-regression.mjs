import fs from 'node:fs';
import assert from 'node:assert/strict';
import {spawnSync} from 'node:child_process';
import {classifyOcrText,CLASSIFIER_SCHEMA,CATEGORY_RULES,tokenMatches} from '../assets/js/data1d-ocr-first-classifier.js';
import {buildAiConsentQueue,AI_CONSENT_SCHEMA} from '../assets/js/data1d1-ocr-region-ai-consent.js';
const paths={classifier:'assets/js/data1d-ocr-first-classifier.js',regionUi:'assets/js/data1d1-ocr-region-ui.js',consent:'assets/js/data1d1-ocr-region-ai-consent.js',wiring:'assets/js/data1d1-ocr-overlay-preview-event-wiring.js',bootstrap:'assets/js/bootstrap.js',worker:'service-worker.js'};for(const path of Object.values(paths)){assert.equal(fs.existsSync(path),true,`missing:${path}`);const checked=spawnSync(process.execPath,['--check',path],{encoding:'utf8'});assert.equal(checked.status,0,`syntax:${path}:${checked.stderr}`);}const source=Object.fromEntries(Object.entries(paths).map(([key,path])=>[key,fs.readFileSync(path,'utf8')]));assert.match(CLASSIFIER_SCHEMA,/^pokemon-sleep-ocr-first-classifier\/1\.[5-9]\d*$/);assert.equal(CATEGORY_RULES.pokemon.tokens.includes('sp'),true);const joint=classifyOcrText('Lv. 31 SP 1789'),levelOnly=classifyOcrText('Lv. 31'),spOnly=classifyOcrText('SP 1789'),strong=classifyOcrText('主技能 Lv. 31');assert.equal(joint.suggested_category,'pokemon');assert.equal(levelOnly.suggested_category,'other');assert.equal(spOnly.suggested_category,'other');assert.equal(strong.suggested_category,'pokemon');assert.ok(joint.classification_confidence>levelOnly.classification_confidence);assert.equal(tokenMatches('display response screenshot inspection','sp'),false);assert.equal(tokenMatches('pokemon sp 1789','sp'),true);const duplicate={sha256:'dup-sha',path:'duplicate.png',status:'duplicate',classification_status:'skipped'},review={sha256:'review-sha',path:'review.png',requires_review:true,classification_status:'suggested'};assert.equal(buildAiConsentQueue([duplicate],{selectedIds:[]}).selected_count,0);const manual=buildAiConsentQueue([duplicate],{selectedIds:['dup-sha']});assert.equal(AI_CONSENT_SCHEMA,'pokemon-sleep-ai-consent-queue/1.2');assert.equal(manual.selected_count,1);assert.equal(manual.manual_duplicate_count,1);assert.equal(manual.items[0].manual_duplicate_override,true);assert.equal(buildAiConsentQueue([duplicate,review],{selectedIds:['dup-sha','review-sha']}).selected_count,2);for(const token of ['ocr-ai-candidate','PREVIEW_ROW_SELECTOR',"archive.readImage(path,{type:'blob'})",'pokemon-sleep:ocr-overlay-preview-requested'])assert.match(source.wiring,new RegExp(token.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));for(const token of ['URL.createObjectURL','URL.revokeObjectURL','ocr-region-preview-image','updatePreviewDom','previousScrollTop','list_position_preserved','ocr_region_preview_rendered','重複圖片：需人工勾選才覆判','duplicate_ai_review_manually_selected','全選一般待覆核','root.dispose'])assert.match(source.regionUi,new RegExp(token.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));assert.doesNotMatch(source.regionUi,/localStorage|sessionStorage|image_base64|btoa\(/);const appVersion=source.bootstrap.match(/^const APP_VERSION\s*=\s*'([^']+)'/m)?.[1],build=source.bootstrap.match(/^const VERSION\s*=\s*'([^']+)'/m)?.[1],cache=source.worker.match(/^const CACHE\s*=\s*'([^']+)'/m)?.[1];assert.equal(appVersion,'v0.3.65');assert.equal(build,'20260803-g13-2g-lightweight-ai-review');assert.equal(cache,'pokemon-sleep-ai-v0.3.65-g13-2g-lightweight-ai-review');console.log(JSON.stringify({ok:true,gate:'SP thumbnail preview compatibility contract',classifier_schema:CLASSIFIER_SCHEMA,app_version:appVersion,build}));