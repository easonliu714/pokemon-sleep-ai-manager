import {isRescueReadonly,requestForcedDatabaseLoad} from './database.js';
import {PUBLIC_ITEM_MASTER} from './public-item-master.js';

const BUILD='20260807-v03952-public-master-version-sync-render-queue';
const STAGING_DB='pokemon_sleep_rescue_import_staging';
const STAGING_STORE='packages';
const esc=value=>String(value??'').replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]));
const $=id=>document.getElementById(id);

function recipes(){return Array.from(globalThis.PokemonSleepPublicRecipeRegistry||[]);}
function ingredients(){const names=new Set();for(const recipe of recipes())for(const part of String(recipe.summary||'').split('、')){const m=part.match(/^(.*)×\d+$/);if(m?.[1])names.add(m[1].trim());}return [...names].sort((a,b)=>a.localeCompare(b,'zh-Hant'));}
function rescueNotice(section,text){if(!section)return;let box=section.querySelector('.v0389-rescue-notice');if(!box){box=document.createElement('div');box.className='panel notice v0389-rescue-notice';section.insertBefore(box,section.children[1]||null);}box.textContent=text;}
function renderIngredients(){const table=$('ingredientTable');if(!table)return;const data=ingredients();rescueNotice($('ingredients'),`救援模式公版：食材 ${data.length} 種。玩家庫存尚未載入，畫面不以 0 代替真實數量。`);table.innerHTML=`<thead><tr><th>食材</th><th>玩家數量</th><th>狀態</th></tr></thead><tbody>${data.map(name=>`<tr><td>${esc(name)}</td><td>尚未載入</td><td>公版唯讀</td></tr>`).join('')}</tbody>`;}
function renderItems(){const table=$('itemTable');if(!table)return;rescueNotice($('items'),`救援模式公版：道具 ${PUBLIC_ITEM_MASTER.length} 種。玩家庫存與保留量尚未載入。`);table.innerHTML=`<thead><tr><th>道具</th><th>分類</th><th>玩家庫存</th><th>功能說明</th></tr></thead><tbody>${PUBLIC_ITEM_MASTER.map(item=>`<tr><td>${esc(item.item_name)}</td><td>${esc(item.item_category)}</td><td>尚未載入</td><td>${esc(item.effect_description_zh_tw)}</td></tr>`).join('')}</tbody>`;}
function renderRecipes(){const table=$('recipeTable');if(!table)return;const data=recipes();rescueNotice($('recipes'),`救援模式公版：料理 ${data.length} 道。解鎖、料理等級與目前能量均屬玩家狀態，目前尚未載入。`);table.innerHTML=`<thead><tr><th>分類</th><th>料理</th><th>配方</th><th>解鎖</th><th>料理等級</th><th>目前能量</th></tr></thead><tbody>${data.map(row=>`<tr><td>${esc(row.category)}</td><td>${esc(row.recipe_name)}</td><td>${esc(row.summary)}</td><td>尚未載入</td><td>尚未載入</td><td>尚未載入</td></tr>`).join('')}</tbody>`;}
function renderRescueCatalog(){if(!isRescueReadonly())return false;renderIngredients();renderItems();renderRecipes();return true;}

function openStageDb(){return new Promise((resolve,reject)=>{const request=indexedDB.open(STAGING_DB,1);request.onupgradeneeded=()=>{const db=request.result;if(!db.objectStoreNames.contains(STAGING_STORE))db.createObjectStore(STAGING_STORE,{keyPath:'update_id'});};request.onsuccess=()=>resolve(request.result);request.onerror=()=>reject(request.error);});}
async function stagePut(payload){const db=await openStageDb();return new Promise((resolve,reject)=>{const tx=db.transaction(STAGING_STORE,'readwrite');tx.objectStore(STAGING_STORE).put({update_id:payload.update_id,payload,staged_at:new Date().toISOString()});tx.oncomplete=()=>{db.close();resolve();};tx.onerror=()=>{db.close();reject(tx.error);};});}
async function stageList(){const db=await openStageDb();return new Promise((resolve,reject)=>{const tx=db.transaction(STAGING_STORE,'readonly');const req=tx.objectStore(STAGING_STORE).getAll();req.onsuccess=()=>{db.close();resolve(req.result||[]);};req.onerror=()=>{db.close();reject(req.error);};});}
async function stageDelete(updateId){const db=await openStageDb();return new Promise((resolve,reject)=>{const tx=db.transaction(STAGING_STORE,'readwrite');tx.objectStore(STAGING_STORE).delete(updateId);tx.oncomplete=()=>{db.close();resolve();};tx.onerror=()=>{db.close();reject(tx.error);};});}
function validatePayload(payload){if(!payload||typeof payload!=='object'||Array.isArray(payload))throw new Error('JSON 根節點必須是物件');for(const key of ['schema_version','update_id','generated_at','operations'])if(!(key in payload))throw new Error(`缺少欄位：${key}`);if(!Array.isArray(payload.operations))throw new Error('operations 必須是陣列');if(payload.operations.length>5000)throw new Error('單包最多 5000 operations');payload.operations.forEach((op,index)=>{if(!op||typeof op!=='object')throw new Error(`操作 ${index} 格式錯誤`);for(const key of ['entity','action','key'])if(!(key in op))throw new Error(`操作 ${index} 缺少 ${key}`);});return {update_id:String(payload.update_id),operation_count:payload.operations.length,entities:[...new Set(payload.operations.map(op=>op.entity))]};}
function ensurePanel(){let panel=document.getElementById('v0389RescueTools');if(panel)return panel;const host=$('updates')||$('dashboard')||document.querySelector('main');if(!host)return null;panel=document.createElement('section');panel.id='v0389RescueTools';panel.className='panel';panel.innerHTML=`<h3>救援模式資料工具</h3><p id="v0389ModeText"></p><div class="button-row"><button id="v0389LoadPlayerDb">載入玩家資料庫並切換一般模式</button><label class="button">選擇 JSON 暫存<input id="v0389JsonFile" type="file" accept="application/json,.json" hidden></label></div><div id="v0389StageMessage" class="notice"></div><div id="v0389StageList"></div>`;host.insertBefore(panel,host.firstChild);return panel;}
async function renderStages(){const panel=ensurePanel();if(!panel)return;const rescue=isRescueReadonly();panel.querySelector('#v0389ModeText').textContent=rescue?'目前為救援／唯讀模式：JSON 僅解析、驗證及暫存，不修改玩家資料。':'一般模式已載入：暫存 JSON 可執行 Dry Run 與套用。';const list=await stageList();const target=panel.querySelector('#v0389StageList');target.innerHTML=list.length?`<h4>待處理 JSON（${list.length}）</h4>${list.map(item=>`<div class="panel v0389-stage" data-id="${esc(item.update_id)}"><b>${esc(item.update_id)}</b>｜${item.payload.operations.length} operations｜${esc(item.staged_at)}<div>${rescue?'載入玩家資料庫後才能 Dry Run／套用。':`<button data-action="dry">Dry Run</button><button data-action="apply">套用</button>`}<button data-action="delete">移除暫存</button></div><pre class="v0389-result"></pre></div>`).join('')}`:'<p>目前沒有暫存 JSON。</p>';target.querySelectorAll('.v0389-stage button').forEach(button=>button.addEventListener('click',async()=>{const card=button.closest('.v0389-stage'),id=card.dataset.id,item=list.find(row=>row.update_id===id),out=card.querySelector('.v0389-result');try{if(button.dataset.action==='delete'){await stageDelete(id);await renderStages();return;}const importer=await import('./importer.js');const preview=importer.dryRun(item.payload);out.textContent=JSON.stringify({operation_count:preview.operation_count,ready_count:preview.ready_count,conflict_count:preview.conflict_count},null,2);if(button.dataset.action==='apply'){if(preview.conflict_count)throw new Error('仍有衝突，禁止套用');if(!confirm(`確認套用 ${preview.operation_count} 筆操作？`))return;await importer.applyPayload(item.payload);await stageDelete(id);window.dispatchEvent(new CustomEvent('pokemon-sleep:data-changed',{detail:{entity:'staged_json',operation:'applied'}}));await renderStages();}}catch(error){out.textContent=`失敗：${error.message}`;}}));}
function installControls(){const panel=ensurePanel();if(!panel)return;panel.querySelector('#v0389LoadPlayerDb').onclick=()=>{if(!confirm('將重新載入頁面並嘗試開啟玩家 SQLite。若載入失敗，下次開啟會自動回到救援模式。是否繼續？'))return;requestForcedDatabaseLoad();location.reload();};panel.querySelector('#v0389JsonFile').onchange=async event=>{const file=event.target.files?.[0];if(!file)return;const message=panel.querySelector('#v0389StageMessage');try{const payload=JSON.parse(await file.text());const summary=validatePayload(payload);await stagePut(payload);message.textContent=`已驗證並暫存：${summary.update_id}，${summary.operation_count} operations；尚未修改玩家資料。`;await renderStages();}catch(error){message.textContent=`JSON 暫存失敗：${error.message}`;}finally{event.target.value='';}};renderStages().catch(console.error);}
function install(){renderRescueCatalog();installControls();for(const button of document.querySelectorAll('nav button'))button.addEventListener('click',()=>setTimeout(()=>{renderRescueCatalog();renderStages().catch(console.error);},0));window.addEventListener('pokemon-sleep:public-recipe-registry-ready',renderRescueCatalog);window.addEventListener('pokemon-sleep:database-ready',()=>{renderRescueCatalog();renderStages().catch(console.error);});}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install,{once:true});else install();
globalThis.PokemonSleepV0389Rescue=Object.freeze({build:BUILD,validatePayload,stagePut,stageList,stageDelete,renderRescueCatalog,public_item_authority:'public-item-master.js'});
