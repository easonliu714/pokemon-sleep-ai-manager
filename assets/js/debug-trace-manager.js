const TRACE_SCHEMA='pokemon-sleep-debug-trace/1.2';
const STORAGE_KEY='pokemon_sleep_debug_trace_v1';
const MODE_KEY='pokemon_sleep_debug_mode_v1';
const MAX_EVENTS=1500;
const MAX_STORAGE_BYTES=1500000;
const PERSIST_DEBOUNCE_MS=350;
const TERMINAL=new Set(['completed','blocked','cancelled','failed','timeout']);
const SENSITIVE=/^(content|data|payload|raw|image|blob|bytes|arrayBuffer|password|token|secret|authorization|api[_-]?key|key)$/i;
const SECRET_CONTROL_IDS=new Set(['aiApiKeysInput','aiApiKeyInput']);
const SECRET_PATTERNS=[/AIza[0-9A-Za-z_-]{20,}/g,/AQ\.[0-9A-Za-z_-]{20,}/g,/Bearer\s+[0-9A-Za-z._-]{12,}/gi];
const uuid=()=>globalThis.crypto?.randomUUID?.()||`dbg-${Date.now()}-${Math.random().toString(16).slice(2)}`;
const now=()=>new Date().toISOString();
const byteLength=value=>new Blob([value]).size;
const localTime=value=>new Intl.DateTimeFormat('zh-TW',{year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',second:'2-digit',fractionalSecondDigits:3,hour12:false,timeZone:Intl.DateTimeFormat().resolvedOptions().timeZone}).format(new Date(value));

function redactString(value){let output=String(value??'');for(const pattern of SECRET_PATTERNS)output=output.replace(pattern,'[redacted-secret]');return output;}
function safeError(error){if(!error)return null;return {name:redactString(error.name||'Error'),message:redactString(error.message||error).slice(0,1000),stack:redactString(error.stack||'').slice(0,5000)};}
function safeFile(file){const name=String(file?.name||'');const ext=(name.match(/\.[^.]+$/)?.[0]||'').toLowerCase();return {name_hint:name?`${name.slice(0,2)}***${ext}`:'',extension:ext,type:String(file?.type||''),size:Number(file?.size||0),last_modified:Number(file?.lastModified||0)};}
function sanitize(value,depth=0,key=''){
  if(depth>5)return '[max-depth]';if(SENSITIVE.test(key))return '[redacted]';if(value instanceof Error)return safeError(value);
  if(typeof File!=='undefined'&&value instanceof File)return safeFile(value);if(typeof FileList!=='undefined'&&value instanceof FileList)return Array.from(value).map(safeFile);
  if(value==null||['string','number','boolean'].includes(typeof value))return typeof value==='string'?redactString(value).slice(0,3000):value;
  if(Array.isArray(value)){const source=key==='events'?value.slice(-MAX_EVENTS):value.slice(0,100);return source.map(item=>sanitize(item,depth+1,key==='events'?'event':key));}
  if(typeof value==='object'){const output={};for(const [childKey,childValue] of Object.entries(value).slice(0,100))output[childKey]=sanitize(childValue,depth+1,childKey);return output;}
  return redactString(value).slice(0,1000);
}
function deepRedact(value){return sanitize(JSON.parse(JSON.stringify(value)));}
function environment(){return {url:location.href.split('#')[0],origin:location.origin,path:location.pathname,user_agent:navigator.userAgent,language:navigator.language,platform:navigator.platform,online:navigator.onLine,display_mode:matchMedia('(display-mode: standalone)').matches?'standalone':'browser',viewport:{width:innerWidth,height:innerHeight,device_pixel_ratio:devicePixelRatio},timezone:Intl.DateTimeFormat().resolvedOptions().timeZone,storage:{local_storage:typeof localStorage!=='undefined',indexed_db:typeof indexedDB!=='undefined'},service_worker:{supported:'serviceWorker'in navigator,controlled:Boolean(navigator.serviceWorker?.controller)}};}

class DebugTraceManager {
  constructor(){this.sessionId=uuid();this.sequence=0;this.events=[];this.operations=new Map();this.mode=localStorage.getItem(MODE_KEY)||'standard';this.startedAt=now();this.listenersInstalled=false;this.persistTimer=null;this.uiTimer=null;this.restore();this.record('app','session_started',{status:'started',environment:environment()});}
  restore(){try{const saved=JSON.parse(localStorage.getItem(STORAGE_KEY)||'[]');if(Array.isArray(saved))this.events=saved.slice(-MAX_EVENTS).map(item=>sanitize(item));}catch{this.events=[];}}
  schedulePersist(){if(this.persistTimer)return;this.persistTimer=setTimeout(()=>{this.persistTimer=null;this.persistNow();},PERSIST_DEBOUNCE_MS);if(!this.uiTimer)this.uiTimer=setTimeout(()=>{this.uiTimer=null;this.refreshUI();},PERSIST_DEBOUNCE_MS);}
  persistNow(){let candidate=this.events.slice(-MAX_EVENTS);while(candidate.length&&byteLength(JSON.stringify(candidate))>MAX_STORAGE_BYTES)candidate=candidate.slice(Math.max(1,Math.floor(candidate.length*.1)));this.events=candidate;try{localStorage.setItem(STORAGE_KEY,JSON.stringify(candidate));}catch(error){console.warn('[DebugTrace] persistence failed',error);}}
  flush(){if(this.persistTimer){clearTimeout(this.persistTimer);this.persistTimer=null;}this.persistNow();this.refreshUI();}
  record(category,event,{status='info',operation_id=null,parent_operation_id=null,details=null,error=null,duration_ms=null,environment:ignoredEnvironment=null}={}){const entry={schema:TRACE_SCHEMA,seq:++this.sequence,timestamp:now(),monotonic_ms:Math.round(performance.now()),session_id:this.sessionId,operation_id,parent_operation_id,category,event,status,page:document.querySelector('.view.active')?.id||null,details:sanitize(details),error:safeError(error),duration_ms};this.events.push(entry);this.schedulePersist();return entry;}
  begin(name,details={},parentOperationId=null){const id=uuid();const started=performance.now();this.operations.set(id,{name,started,parentOperationId});this.record('operation',name,{status:'started',operation_id:id,parent_operation_id:parentOperationId,details});return id;}
  recordStage(operationId,stage,details={}){return this.record('business_stage',stage,{status:'completed',operation_id:operationId,details});}
  recordProgress(operationId,stage,current,total,details={}){if(this.mode!=='detailed'&&current!==0&&current!==total)return null;return this.record('progress',stage,{status:'progress',operation_id:operationId,details:{current,total,percent:total?Math.round(current/total*100):null,...details}});}
  end(id,status='completed',details={}){if(!TERMINAL.has(status))throw new Error(`invalid_terminal_status:${status}`);const op=this.operations.get(id);const duration=op?Math.round(performance.now()-op.started):null;this.record('operation',op?.name||'unknown_operation',{status,operation_id:id,parent_operation_id:op?.parentOperationId||null,details,duration_ms:duration});this.operations.delete(id);}
  fail(id,error,details={}){const op=this.operations.get(id);const duration=op?Math.round(performance.now()-op.started):null;this.record('operation',op?.name||'unknown_operation',{status:'failed',operation_id:id,parent_operation_id:op?.parentOperationId||null,details,error,duration_ms:duration});this.operations.delete(id);}
  setMode(mode){if(!['standard','detailed'].includes(mode))throw new Error('invalid_debug_mode');this.mode=mode;localStorage.setItem(MODE_KEY,mode);this.record('diagnostic','mode_changed',{status:'completed',details:{mode}});}
  captureState(label,state){if(this.mode!=='detailed')return null;return this.record('state',label,{status:'snapshot',details:state});}
  buildReport(){this.flush();const openOperations=Array.from(this.operations.entries()).map(([id,op])=>({operation_id:id,name:op.name,age_ms:Math.round(performance.now()-op.started),parent_operation_id:op.parentOperationId}));return deepRedact({schema:TRACE_SCHEMA,exported_at:now(),app:{version:document.documentElement.dataset.appVersion||null,build:document.documentElement.dataset.appBuild||null},session:{id:this.sessionId,started_at:this.startedAt,mode:this.mode},environment:environment(),summary:this.summary(),open_operations:openOperations,events:this.events});}
  downloadJson(payload,name){const redacted=deepRedact(payload);const serialized=JSON.stringify(redacted,null,2);const blob=new Blob([serialized],{type:'application/json'});const link=document.createElement('a');link.href=URL.createObjectURL(blob);link.download=name;link.click();setTimeout(()=>URL.revokeObjectURL(link.href),1000);}
  export(){const report=this.buildReport();this.downloadJson(report,`pokemon_sleep_debug_${report.app.version||'unknown'}_${this.sessionId}_${new Date().toISOString().replace(/[:.]/g,'-')}.json`);this.record('diagnostic','trace_exported',{status:'completed',details:{event_count:this.events.length,open_operation_count:report.open_operations.length,secret_redaction:true}});}
  exportIssueBundle(){const report=this.buildReport();const bundle={bundle_schema:'pokemon-sleep-issue-bundle/1.1',generated_at:now(),trace:report,environment:report.environment,storage_summary:{trace_event_count:this.events.length,trace_storage_limit_bytes:MAX_STORAGE_BYTES,trace_event_limit:MAX_EVENTS},service_worker:report.environment.service_worker,security:{secret_redaction:true}};this.downloadJson(bundle,`pokemon_sleep_issue_bundle_${report.app.version||'unknown'}_${this.sessionId}_${new Date().toISOString().replace(/[:.]/g,'-')}.json`);this.record('diagnostic','issue_bundle_exported',{status:'completed',details:{secret_redaction:true}});}
  clear(){this.events=[];this.operations.clear();localStorage.removeItem(STORAGE_KEY);this.record('diagnostic','trace_cleared',{status:'completed'});}
  summary(){const current=this.events.filter(e=>e.session_id===this.sessionId);return {event_count:current.length,warning_count:current.filter(e=>e.status==='warning').length,error_count:current.filter(e=>e.status==='failed'||e.error).length,last_event:current.at(-1)?.event||null,last_error:[...current].reverse().find(e=>e.status==='failed'||e.error)||null};}
  installGlobalListeners(){if(this.listenersInstalled)return;this.listenersInstalled=true;addEventListener('error',event=>this.record('runtime','window_error',{status:'failed',details:{filename:event.filename,lineno:event.lineno,colno:event.colno},error:event.error||event.message}),true);addEventListener('unhandledrejection',event=>this.record('runtime','unhandled_rejection',{status:'failed',error:event.reason}),true);addEventListener('online',()=>this.record('network','online',{status:'completed'}));addEventListener('offline',()=>this.record('network','offline',{status:'warning'}));document.addEventListener('visibilitychange',()=>this.record('app','visibility_changed',{status:'completed',details:{visibility:document.visibilityState}}));document.addEventListener('click',event=>{const target=event.target?.closest?.('button,a,[role="button"]');if(!target)return;this.record('ui','control_clicked',{status:'completed',details:{id:target.id||null,label:(target.textContent||target.getAttribute('aria-label')||'').trim().slice(0,120),view:target.closest('.view')?.id||null,disabled:Boolean(target.disabled)}});},true);document.addEventListener('change',event=>{const target=event.target;if(!target)return;const secret=SECRET_CONTROL_IDS.has(target.id)||target.type==='password'||/key|secret|token|password/i.test(target.name||'');const details={id:target.id||null,type:target.type||target.tagName,value_kind:target.type==='file'?'file_selection':secret?'secret_control_change':'control_change'};if(target.type==='file')details.files=Array.from(target.files||[]).map(safeFile);else if(secret){const lines=String(target.value||'').split(/[\n,]+/).map(value=>value.trim()).filter(Boolean);details.redacted=true;details.value_length=String(target.value||'').length;details.key_count=lines.length;}else if(this.mode==='detailed')details.value=redactString(target.value||'').slice(0,200);this.record('ui','control_changed',{status:'completed',details});},true);document.addEventListener('submit',event=>this.record('ui','form_submitted',{status:'completed',details:{id:event.target?.id||null}}),true);navigator.serviceWorker?.addEventListener('controllerchange',()=>this.record('service_worker','controller_changed',{status:'completed',details:{controlled:Boolean(navigator.serviceWorker.controller)}}));}
  installUI(){if(document.getElementById('diagnostics'))return;const nav=document.querySelector('nav');const main=document.querySelector('main');if(!nav||!main)return;const button=document.createElement('button');button.dataset.view='diagnostics';button.textContent='診斷中心';nav.append(button);const section=document.createElement('section');section.id='diagnostics';section.className='view';section.innerHTML=`<div class="section-head"><h2>診斷中心</h2><span class="badge" id="debugModeBadge"></span></div><div class="cards"><article><strong id="debugSession">—</strong><span>Session</span></article><article><strong id="debugEventCount">0</strong><span>事件</span></article><article><strong id="debugErrorCount">0</strong><span>錯誤</span></article><article><strong id="debugLastEvent">—</strong><span>最後事件</span></article></div><div class="panel"><p id="debugModeHelp"></p><p id="debugLastError">目前沒有錯誤。</p><div class="buttons"><button id="debugExportBtn">匯出診斷 JSON</button><button id="debugBundleBtn">匯出 Issue Bundle</button><button id="debugModeBtn">切換為明細模式</button><button id="debugClearBtn" class="danger">清除診斷紀錄</button></div><p class="notice">標準模式適合日常常駐；明細模式適合重現問題。API Key、Token 與密碼欄位永遠只記錄遮罩摘要。</p></div><h3>最近事件</h3><div class="table-wrap"><table id="debugEventTable"></table></div>`;main.append(section);button.addEventListener('click',()=>{document.querySelectorAll('nav button').forEach(x=>x.classList.toggle('active',x===button));document.querySelectorAll('.view').forEach(x=>x.classList.toggle('active',x===section));this.record('navigation','view_changed',{status:'completed',details:{view:'diagnostics'}});this.refreshUI();});section.querySelector('#debugExportBtn').addEventListener('click',()=>this.export());section.querySelector('#debugBundleBtn').addEventListener('click',()=>this.exportIssueBundle());section.querySelector('#debugModeBtn').addEventListener('click',()=>this.setMode(this.mode==='standard'?'detailed':'standard'));section.querySelector('#debugClearBtn').addEventListener('click',()=>this.clear());this.refreshUI();}
  refreshUI(){const summary=this.summary();const set=(id,value)=>{const node=document.getElementById(id);if(node)node.textContent=value;};const modeLabel=this.mode==='standard'?'標準':'明細';set('debugSession',this.sessionId.slice(0,12));set('debugEventCount',summary.event_count);set('debugErrorCount',summary.error_count);set('debugLastEvent',summary.last_event||'—');set('debugModeBadge',`目前紀錄模式：${modeLabel}`);set('debugModeHelp',this.mode==='standard'?'標準模式：保留主要操作、階段與錯誤，適合日常持續使用。':'明細模式：額外保留控制摘要、狀態快照與較密集進度；祕密欄位仍強制遮罩。');set('debugLastError',summary.last_error?`最後錯誤：${summary.last_error.event}／${summary.last_error.error?.message||summary.last_error.status}`:'目前沒有錯誤。');set('debugModeBtn',this.mode==='standard'?'切換為明細模式':'切換為標準模式');const table=document.getElementById('debugEventTable');if(table){const rows=this.events.filter(e=>e.session_id===this.sessionId).slice(-50).reverse();table.innerHTML='<thead><tr><th>當地時間</th><th>分類</th><th>事件</th><th>狀態</th><th>操作 ID</th></tr></thead><tbody>'+rows.map(e=>`<tr><td>${localTime(e.timestamp)}</td><td>${e.category}</td><td>${e.event}</td><td>${e.status}</td><td>${e.operation_id?.slice(0,8)||'—'}</td></tr>`).join('')+'</tbody>';}}
}

export const debugTrace=new DebugTraceManager();debugTrace.installGlobalListeners();addEventListener('DOMContentLoaded',()=>debugTrace.installUI(),{once:true});globalThis.DebugTrace=debugTrace;export {DebugTraceManager,sanitize as sanitizeDebugValue,safeFile as summarizeDebugFile,redactString};
